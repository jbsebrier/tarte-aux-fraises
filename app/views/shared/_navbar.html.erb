<div class="navbar-wagon navbar-wagon-fixed navbar-<%= controller.controller_name %> navbar-<%= controller.controller_name %>-<%= action_name %>">
  <!-- Logo -->
  <%= link_to root_path, class: "navbar-wagon-brand" do %>
    <%= image_tag "icon.png" %>
  <% end %>

  <!-- Right Navigation -->
  <div class="navbar-wagon-right hidden-xs hidden-sm">

    <!-- Create an event -->
    <%= link_to t(".sign_in", default: "Organiser un dîner"),
      new_event_path, class: "navbar-item navbar-wagon-link" %>

    <!-- Different navigation if login or not -->
    <% if couple_signed_in? %>

      <!-- Profile picture and dropdown -->
      <div class="navbar-item">
        <div class="dropdown">
          <%= image_tag %>
          <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
            <li>
              <%= link_to my_profile_path(current_couple) do %>
                <i class="fa fa-user"></i> <%= t(".profile", default: "Notre profil") %>
              <% end %>
            </li>
            <li>
              <%= link_to root_path do %>
                <i class="fa fa-home"></i>  <%= t(".profile", default: "Page d'accueil") %>
              <% end %>
            </li>
            <li>
              <%= link_to destroy_couple_session_path, method: :delete do %>
                <i class="fa fa-sign-out"></i>  <%= t(".sign_out", default: "Fermer la session") %>
              <% end %>
            </li>
          </ul>
        </div>
      </div>

    <% else %>

      <!-- Login text link -->
      <%= link_to t(".sign_in", default: "Se connecter / S'inscrire"), new_couple_session_path, class: "navbar-item navbar-wagon-link" %>

    <% end %>

  </div>

  <!-- Dropdown list appearing on mobile only -->
  <div class="navbar-item hidden-md hidden-lg">
    <div class="dropdown">
      <i class="fa fa-bars dropdown-toggle" data-toggle="dropdown"></i>
      <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
        <% if couple_signed_in? %>
          <li>
            <%= link_to my_profile_path(current_couple) do %>
              <i class="fa fa-user"></i> <%= t(".profile", default: "Notre profil") %>
            <% end %>
          </li>
          <li>
            <%= link_to root_path do %>
              <i class="fa fa-home"></i>  <%= t(".profile", default: "Page d'accueil") %>
            <% end %>
          </li>
          <li>
            <%= link_to destroy_couple_session_path, method: :delete do %>
              <i class="fa fa-sign-out"></i>  <%= t(".sign_out", default: "Fermer la session") %>
            <% end %>
          </li>
         <% else %>
           <!-- Login text link -->
           <%= link_to t(".sign_in", default: "Se connecter / S'inscrire"), new_couple_session_path, class: "navbar-item navbar-wagon-link" %>
        <% end %>
      </ul>
    </div>
  </div>
</div>
